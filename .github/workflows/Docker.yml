name: 6 ==> Docker Build & Push
on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: 'Branche où les artifacts ont été générés (ex: develop)'
        required: false
        default: ''
        type: string

