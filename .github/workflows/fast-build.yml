name: Fast Build

on:
  workflow_dispatch:  # Lancement manuel uniquement
    inputs:
      branch:
        description: 'Branche à utiliser'
        required: false
        default: 'main'  # Remplacez par votre branche par défaut (main/master/develop)
        type: string

env:
  CARGO_TERM_COLOR: always

jobs:
  backend-build:
    name: 🏗️ Backend (Release)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Installer Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-release-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-release-
            ${{ runner.os }}-cargo-

      - name: Créer base de données temporaire (pour sqlx offline)
        run: |
          mkdir -p data
          touch data/temp.db
          echo "Base de données temporaire créée"

      - name: Installer sqlx-cli
        run: cargo install sqlx-cli --version 0.8.6 --no-default-features --features sqlite --locked

      - name: Préparer cache SQLx
        env:
          DATABASE_URL: "sqlite:data/temp.db"
        run: |
          # Exécuter les migrations si elles existent
          if [ -d "migrations" ] && [ "$(ls -A migrations 2>/dev/null)" ]; then
            sqlx migrate run
          fi
          # Préparer le cache SQLx
          cargo sqlx prepare -- --all-targets

      - name: Build Release (offline)
        env:
          SQLX_OFFLINE: true
        run: cargo build --release

      - name: Vérifier le binaire
        run: |
          ls -la target/release/
          echo "✅ Build backend réussi!"

  frontend-build:
    name: 🎨 Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Installer dépendances frontend
        working-directory: ./frontend
        run: npm ci

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

      - name: Vérifier le build
        working-directory: ./frontend
        run: |
          if [ -d "dist" ] || [ -d "build" ] || [ -d "out" ]; then
            echo "✅ Build frontend réussi!"
          else
            echo "⚠️ Aucun répertoire de build trouvé, mais c'est peut-être normal selon votre configuration"
          fi