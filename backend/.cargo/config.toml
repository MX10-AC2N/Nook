# Configuration de compilation cross aarch64 via gcc
# x86_64: cible native du runner (pas besoin de config)

[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

[profile.release]
lto = true
codegen-units = 1
opt-level = "z"
strip = true
